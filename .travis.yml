language: php
sudo: required
php:
  - '5.5'
  - '7.0'
services:
  - rabbitmq
before_script:
  - composer install
  - echo "extension = <extension>.so" >> ~/.phpenv/versions/$(phpenv version-name)/etc/php.ini
script:
  - ./bot.php
  - ./bot.php status